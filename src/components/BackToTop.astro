---
// No build-time logic needed
---

<button
  id="back-to-top"
  class="fixed top-0 right-0 z-[60] flex flex-col items-end gap-2 opacity-0 pointer-events-none translate-y-[-10px] transition-all duration-300 ease-out"
  aria-label="Наверх"
>
  <!-- Left (longer) cord hanging from top -->
  <div class="flex flex-col items-center">
    <div class="w-[3px] h-[60vh] bg-[#7ABEFF] rounded-full"></div>
    <div class="w-3 h-3 rounded-full bg-white mt-0.5" style="box-shadow: 0 0 8px rgba(255,255,255,0.6);"></div>
  </div>
  <!-- Right (shorter) cord hanging from top -->
  <div class="flex flex-col items-center">
    <div class="w-[3px] h-[45vh] bg-[#7ABEFF] rounded-full"></div>
    <div class="w-3 h-3 rounded-full bg-white mt-0.5" style="box-shadow: 0 0 8px rgba(255,255,255,0.6);"></div>
  </div>
</button>

<script>
  const btn = document.getElementById('back-to-top');
  if (!btn) throw new Error('[BackToTop] button not found');

  let visible = false;

  function show() {
    if (visible) return;
    visible = true;
    btn.classList.remove('opacity-0', 'pointer-events-none', 'translate-y-[-10px]');
    btn.classList.add('opacity-100', 'translate-y-0');
  }

  function hide() {
    if (!visible) return;
    visible = false;
    btn.classList.add('opacity-0', 'pointer-events-none', 'translate-y-[-10px]');
    btn.classList.remove('opacity-100', 'translate-y-0');
  }

  window.addEventListener('scroll', () => {
    if (window.scrollY > 300) show();
    else hide();
  });

  btn.addEventListener('click', () => {
    btn.classList.add('animate-btt-pull');
    btn.style.transitionDuration = '0ms';

    window.scrollTo({ top: 0, behavior: 'smooth' });

    setTimeout(() => {
      btn.classList.remove('animate-btt-pull');
      btn.style.transitionDuration = '';
    }, 350);
  });
</script>

<style>
  @keyframes btt-pull {
    0%   { transform: translateY(0); }
    25%  { transform: translateY(-10px); }
    55%  { transform: translateY(3px); }
    100% { transform: translateY(0); }
  }
  .animate-btt-pull {
    animation: btt-pull 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
</style>